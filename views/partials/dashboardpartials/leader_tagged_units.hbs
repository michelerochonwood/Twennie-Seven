<!-- CSRF token for AJAX deletes -->
<meta name="csrf-token" content="{{csrfToken}}">

{{!-- =========================
     1) SELF-TAGGED (BLENDED)
     expects: leaderSelfTaggedUnits = [{ _id, title, mainTopic, unitType, tagId, viewPath }]
     ========================= --}}
<section class="tagged-units-section">
  <h2 class="tagged-units-title">tagged library units</h2>
  <div class="tagged-units-grid">
    {{#if leaderSelfTaggedUnits.length}}
      {{#each leaderSelfTaggedUnits}}
        <div class="tagged-unit-card">
          <div class="tagged-unit-icon">
            <img src="/icons/{{unitType}}.svg" alt="{{unitType}} icon">
          </div>

          <div class="tagged-unit-info">
            <h3 class="tagged-unit-title">{{title}}</h3>
            {{#if mainTopic}}<p class="tagged-unit-topic">{{mainTopic}}</p>{{/if}}
          </div>

          <div class="tagged-unit-actions" style="display:flex; flex-direction:column; align-items:center; gap:6px; margin-top:6px;">
            <a href="{{viewPath}}" class="tagged-unit-link">view unit</a>

            <button class="tagged-unit-link"
              onclick="removeTagFromUnit('{{tagId}}','{{_id}}','{{unitType}}')">
              remove from this unit
            </button>

            <button class="tagged-unit-link"
              onclick="deleteTag('{{tagId}}')">
              delete tag
            </button>
          </div>
        </div>
      {{/each}}
    {{else}}
      <p class="tagged-no-units">You have no self-tagged units.</p>
    {{/if}}
  </div>
</section>

<div class="tagged-section-divider" aria-hidden="true"></div>

{{!-- =========================
     3) ASSIGNED UNITS (NON-NUGGETS ONLY)
     expects: leaderAssignedNonNuggetUnits = [{ _id, title, mainTopic, unitType, tagId, assignedToName, assignedCompletedAtFormatted, assignedInstructions, viewPath }]
     ========================= --}}
<section class="leader-assigned-units-section" style="margin-top:20px">
  <h2 class="tagged-units-title">assigned units</h2>
  <div class="tagged-units-grid">
    {{#if leaderAssignedNonNuggetUnits.length}}
      {{#each leaderAssignedNonNuggetUnits}}
        <div class="leader-assigned-unit-card">
          <div class="tagged-unit-icon">
            <img src="/icons/{{unitType}}.svg" alt="{{unitType}} icon">
          </div>

          <div class="tagged-unit-info">
            <h3 class="tagged-unit-title">{{title}}</h3>
            {{#if mainTopic}}<p class="tagged-unit-topic">{{mainTopic}}</p>{{/if}}

            <p class="leader-assigned-to">assigned to: {{assignedToName}}</p>

            {{#if assignedCompletedAtFormatted}}
              <p class="leader-assigned-status">✅ completed <span class="muted">({{assignedCompletedAtFormatted}})</span></p>
            {{else}}
              <p class="leader-assigned-status">⏳ pending</p>
            {{/if}}

            {{#if assignedInstructions}}
              <p class="leader-assigned-instructions"><em>{{assignedInstructions}}</em></p>
            {{/if}}
          </div>

          <div class="tagged-unit-actions" style="display:flex; flex-direction:column; align-items:center; gap:6px; margin-top:6px;">
            <a href="{{viewPath}}" class="tagged-unit-link">view unit</a>

            <button class="tagged-unit-link"
              onclick="unassignMember('{{tagId}}','{{assignedToId}}')">
              unassign
            </button>

            <button class="tagged-unit-link"
              onclick="deleteTag('{{tagId}}')">
              delete tag
            </button>
          </div>
        </div>
      {{/each}}
    {{else}}
      <p class="tagged-no-units">You have no assigned non-nugget units.</p>
    {{/if}}
  </div>
</section>

<div class="tagged-section-divider" aria-hidden="true"></div>

{{!-- =========================
     4) ASSIGNED NUGGETS (NUGGETS ONLY)
     expects: leaderAssignedNuggets = [{ _id, title, client, region, discipline, tagId, assignedToName, assignedCompletedAtFormatted, assignedInstructions }]
     ========================= --}}
<section class="leader-assigned-nuggets-section" style="margin-top:20px">
  <h2 class="tagged-units-title">assigned nuggets</h2>
  <div class="tagged-units-grid">
    {{#if leaderAssignedNuggets.length}}
      {{#each leaderAssignedNuggets}}
        <div class="leader-assigned-unit-card">
          <div class="tagged-unit-icon">
            <img src="/icons/nugget.svg" alt="nugget icon">
          </div>

          <div class="tagged-unit-info">
            <h3 class="tagged-unit-title">{{title}}</h3>
            {{#if client}}<p class="tagged-unit-topic">client: {{client}}</p>{{/if}}
            {{#if region}}<p class="tagged-unit-topic">region: {{region}}</p>{{/if}}
            {{#if discipline}}<p class="tagged-unit-topic">discipline: {{discipline}}</p>{{/if}}

            <p class="leader-assigned-to">assigned to: {{assignedToName}}</p>

            {{#if assignedCompletedAtFormatted}}
              <p class="leader-assigned-status">✅ completed <span class="muted">({{assignedCompletedAtFormatted}})</span></p>
            {{else}}
              <p class="leader-assigned-status">⏳ pending</p>
            {{/if}}

            {{#if assignedInstructions}}
              <p class="leader-assigned-instructions"><em>{{assignedInstructions}}</em></p>
            {{/if}}
          </div>

          <div class="tagged-unit-actions" style="display:flex; flex-direction:column; align-items:center; gap:6px; margin-top:6px;">
            <a href="/unitviews/nuggets/view/{{_id}}" class="tagged-unit-link">view nugget</a>

            <button class="tagged-unit-link"
              onclick="unassignMember('{{tagId}}','{{assignedToId}}')">
              unassign
            </button>

            <button class="tagged-unit-link"
              onclick="deleteTag('{{tagId}}')">
              delete tag
            </button>
          </div>
        </div>
      {{/each}}
    {{else}}
      <p class="tagged-no-units">You have no assigned nuggets.</p>
    {{/if}}
  </div>
</section>

<script>
  function getCsrfToken() {
    return document.querySelector('meta[name="csrf-token"]')?.getAttribute('content');
  }

  async function removeTagFromUnit(tagId, unitId, unitType) {
    if (!confirm("Remove this tag from the unit?")) return;
    try {
      const res = await fetch(`/tags/remove/${tagId}/${unitId}/${unitType}`, {
        method: 'DELETE',
        headers: { 'X-CSRF-Token': getCsrfToken() }
      });
      const data = await res.json().catch(() => ({}));
      if (res.ok) location.reload(); else alert(data.message || 'Failed to remove tag.');
    } catch (err) {
      console.error('removeTagFromUnit error:', err);
      alert('An error occurred while removing the tag.');
    }
  }

  async function unassignMember(tagId, memberId) {
    if (!confirm("Unassign this member from the tag?")) return;
    try {
      const res = await fetch(`/tags/${tagId}/assigned/${memberId}`, {
        method: 'DELETE',
        headers: { 'X-CSRF-Token': getCsrfToken() }
      });
      const data = await res.json().catch(() => ({}));
      if (res.ok) location.reload(); else alert(data.message || 'Could not unassign.');
    } catch (err) {
      console.error('unassignMember error:', err);
      alert('An error occurred while unassigning.');
    }
  }

  async function deleteTag(tagId) {
    if (!confirm("Delete this tag entirely? This removes it from all units and all assignees.")) return;
    try {
      const res = await fetch(`/tags/${tagId}`, {
        method: 'DELETE',
        headers: { 'X-CSRF-Token': getCsrfToken() }
      });
      const data = await res.json().catch(() => ({}));
      if (res.ok) location.reload(); else alert(data.message || 'Failed to delete tag.');
    } catch (err) {
      console.error('deleteTag error:', err);
      alert('An error occurred while deleting the tag.');
    }
  }
</script>









